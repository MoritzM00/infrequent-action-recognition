# Full evaluation with chain-of-thought on Wanfall
# @package _global_

defaults:
  - override /model: qwen3-vl-8b
  - override /dataset: wanfall
  - override /inference: cot

# Full dataset evaluation
num_samples: null  # Process all samples
output_dir: outputs/wanfall_cot_full
save_predictions: true
save_metrics: true
verbose: 5
