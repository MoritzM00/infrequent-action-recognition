# Quick test experiment with small model and limited samples
# @package _global_

defaults:
  - override /model: qwen3-vl-4b
  - override /dataset: omnifall
  - override /inference: default

# Override settings for quick testing
num_samples: 10
output_dir: outputs/quick_test
verbose: 10  # Print more samples for debugging
