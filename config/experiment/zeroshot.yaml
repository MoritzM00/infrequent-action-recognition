# @package _global_

defaults:
  - /dataset/omnifall/video@dataset: oops
  - override /prompt: default

# Full dataset evaluation
num_samples: null  # Process all samples

# W&B tracking
wandb:
  project: "fall-detection-zeroshot-v3"
  name:
  tags: ["zeroshot"]

# Output settings
output_dir: "outputs/zeroshot-v3"
