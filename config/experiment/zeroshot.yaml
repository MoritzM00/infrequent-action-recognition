# @package _global_

defaults:
  - /dataset/omnifall/video@dataset: oops

# Full dataset evaluation
num_samples: null  # Process all samples

# Model configuration
model:
  params: "4B"  # Use 4B model for zero-shot

# W&B tracking
wandb:
  project: "fall-detection-zeroshot-v2"
  name:
  tags: ["zeroshot"]

# Output settings
output_dir: "outputs/zeroshot-v2"
