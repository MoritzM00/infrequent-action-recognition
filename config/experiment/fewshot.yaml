# @package _global_

defaults:
  - /dataset/omnifall/video@dataset: oops
  - override /prompt: fewshot

# Full dataset evaluation
num_samples: null

vllm:
  mm_processor_cache_gb: 4

# W&B tracking
wandb:
  project: "fall-detection-fewshot"
  name:
  tags: ["fewshot"]

# Output settings
output_dir: "outputs/fewshot"
