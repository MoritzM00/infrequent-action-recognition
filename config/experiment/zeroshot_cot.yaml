# @package _global_

defaults:
  - /dataset/omnifall/video@dataset: oops
  - override /model: qwenvl
  - override /sampling: qwen3_thinking
  - override /prompt: cot


model:
  variant: thinking
  params: 8B

# Full dataset evaluation
num_samples: null  # Process all samples

# W&B tracking
wandb:
  project: "fall-detection-zeroshot-cot"
  name:
  tags: ["zeroshot", "cot"]
  mode: online

# Output settings
output_dir: "outputs/zeroshot-cot"
