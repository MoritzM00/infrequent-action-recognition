# Model comparison: Run same dataset on different models
# @package _global_

defaults:
  - override /model: qwen3-vl-4b  # Change this for different runs
  - override /dataset: omnifall
  - override /inference: default

# Consistent settings for fair comparison
num_samples: null
output_dir: outputs/model_comparison
save_predictions: true
save_metrics: true
verbose: 0  # No verbose output for clean comparison

# Can override model like:
# python scripts/vllm_inference.py +experiment=model_comparison model=qwen3-vl-8b
