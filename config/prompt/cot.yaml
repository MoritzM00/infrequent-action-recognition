output_format: text
cot: true
cot_start_tag: "<think>"
cot_end_tag: "</think>"
model_family: ${model.family}

# Variant selectors
role_variant: standard
task_variant: standard
labels_variant: bulleted
definitions_variant: null

# Few-shot settings (0 = zero-shot)
num_shots: 0
shot_selection: balanced
exemplar_seed: 42
