# Default prompt configuration
# Uses all components: role, labels, definitions, constraints
# Output format: JSON

output_format: json
include_role: true
include_label_definitions: true
include_constraints: true
cot: false
cot_start_tag: "<think>"
cot_end_tag: "</think>"
few_shot_examples: null
model_family: ${model.family}
