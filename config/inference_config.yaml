defaults:
  - vllm: default
  - sampling: greedy
  - model: qwen/instruct
  - /dataset/omnifall/video/oops@dataset
  - inference: default
  - _self_

# Global video processing parameters
model_fps: 8.0  # Target frame rate for video sampling
num_frames: 16  # Number of frames to extract per video

# Output configuration
output_dir: "outputs"
save_predictions: true
save_metrics: true

# Inference settings
num_samples: null  # null = all samples
verbose: 5  # Number of samples to print

# Hydra configuration
hydra:
  job:
    chdir: false  # Keep working directory for inference
