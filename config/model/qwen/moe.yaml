# Qwen MoE models

version: "3"
variant: "Instruct"
total_params: "30B"
active_params: "A3B"

name: "Qwen${model.version}-VL-${model.total_params}-${model.active_params}-${model.variant}"
checkpoint_path: "Qwen/${model.name}"

# Override vLLM settings for MoE
vllm:
  enable_expert_parallel: true
