# Qwen (Thinking) models

# Model identification
family: "Qwen"
version: "3"
variant: "Thinking"
params: "4B"

# Computed paths
name: "${model.family}${model.version}-${model.variant}"
checkpoint_path: "${model.family}/${model.name}"

needs_video_metadata: true
