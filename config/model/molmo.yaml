org: allenai
family: Molmo
version: 2
variant: null
params: 4B  # Total parameters (4 or 8B)

# MoE-specific (set for MoE models, leave null for standard)
active_params: null  #  not supported for Molmo

needs_video_metadata: true

mm_processor_kwargs:
  do_resize: true
  use_fast: true

# note: molmo needs Channels last format as input tensors for video, but dataset returns channels first
